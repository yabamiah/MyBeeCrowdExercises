#include <stdio.h>

int main()
{
  int casos, comp, radA,radQ;

  scanf("%d", &casos);

  while(casos--) {
	 scanf("%d %d", &comp, &radA);

    radQ = (comp / radA) + 1;
  
	 printf("%d\n", radQ);
  }
  
  return 0;
}
