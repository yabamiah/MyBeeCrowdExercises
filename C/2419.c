#include <stdio.h>

int main()
{
  int lin, col;
  scanf("%d %d", &lin, &col);

  char coord[lin][col];

  for(int i = 0; i < lin; ++i){
    for(int j = 0; j < col; ++j){
      scanf("%c", &coord[i][j]);
    }
  }

  for

  return 0;
}
