#include <stdio.h>

int main()
{
  int casos,hex, dec, bin;
  char num[255], base[4];
  
  while(casos)
  {
	 scanf("%s", num);
	 scanf("%s", base);

	 if (base == "hex")
	 {
		
	 }

	 casos --;
  }

  return 0;
}


int hex_convert_dec(int hexa)
{
  
}
